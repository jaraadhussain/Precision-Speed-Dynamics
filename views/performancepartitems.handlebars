{{> navbar}}
<div class="style-part-items-container">
    {{#if_eq category "DOWNPIPES"}}
        {{#each inventory.DOWNPIPES}}
        <div class="style-card">
            <img src="{{img}}" alt="{{key}}">
            <h2>{{name}}</h2>
            <p>Price: {{price}}</p>
            <p>In Stock: {{quantity}}</p>
            <button type="submit"><i class="fas fa-shopping-cart"></i></button>
        </div>
        {{/each}}
    {{/if_eq}}

    {{#if_eq category "EXHAUSTS"}}
        {{#each inventory.EXHAUSTS}}
        <div class="style-card">
            <img src="{{img}}" alt="{{key}}">
            <h2>{{short_name}}</h2>
            <p>Price: {{price}}</p>
            <p>In Stock: {{quantity}}</p>
            <button type="submit"><i class="fas fa-shopping-cart"></i></button>
        </div>
        {{/each}}
    {{/if_eq}}

    {{#if_eq category "TURBO_KITS"}}
        {{#each inventory.TURBO_KITS}}
        <div class="style-card">
            <img src="{{img}}" alt="{{key}}">
            <h2>{{name}}</h2>
            <p>Price: {{price}}</p>
            <p>In Stock: {{quantity}}</p>
            <button type="submit"><i class="fas fa-shopping-cart"></i></button>
        </div>
        {{/each}}
    {{/if_eq}}

    {{#if_eq category "INTAKES"}}
        {{#each inventory.INTAKES}}
        <div class="style-card">
            <img src="{{img}}" alt="{{key}}">
            <h2>{{name}}</h2>
            <p>Price: {{price}}</p>
            <p>In Stock: {{quantity}}</p>
            <button type="submit"><i class="fas fa-shopping-cart"></i></button>
        </div>
        {{/each}}
    {{/if_eq}}

    {{#if_eq category "INTERCOOLERS"}}
        {{#each inventory.INTERCOOLERS}}
        <div class="style-card">
            <img src="{{img}}" alt="{{key}}">
            <h2>{{name}}</h2>
            <p>Price: {{price}}</p>
            <p>In Stock: {{quantity}}</p>
            <button type="submit"><i class="fas fa-shopping-cart"></i></button>
        </div>
        {{/each}}
    {{/if_eq}}

    {{#if_eq category "TUNING"}}
        {{#each inventory.TUNING}}
        <div class="style-card">
            <img src="{{img}}" alt="{{key}}">
            <h2>{{name}}</h2>
            <p>Price: {{price}}</p>
            <p>In Stock: {{quantity}}</p>
            <button type="submit"><i class="fas fa-shopping-cart"></i></button>
        </div>
        {{/each}}
    {{/if_eq}}
</div>
